# Database
# Password untuk PostgreSQL container (ganti dengan password kuat)
DB_PASSWORD=royalkomputer2026

# Application
APP_ENV=production

# Git Backup (opsional — untuk backup data dari backend ke GitHub)
# Buat GitHub Personal Access Token dengan scope: repo (Contents write + Metadata read)
# GIT_TOKEN=github_pat_xxxxxxxxxxxx
# GIT_REPO_URL=https://github.com/royalkomputer/website.git
# GIT_BRANCH=main
